{
  "name": "time",
  "version": "0.1.0",
  "description": "ticking clock, reports a time every specified interval",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "dependencies": {
    "dateformat": "~1.0.11"
  }
}
